<div class="gmcq-inner component-inner">
    {{#if title}}
    <div class="gmcq-title component-title">
        <h4 class="gmcq-title-inner component-title-inner">
            {{{title}}}
        </h4>
    </div>
    {{/if}}
    {{#if body}}
    <div class="gmcq-body component-body">
        <div class="gmcq-body-inner component-body-inner">
            {{{body}}}
        </div>
    </div>
    {{/if}}
    <div class="gmcq-widget component-widget {{#unless _isEnabled}} disabled {{#if _isComplete}} complete submitted user {{#if _isCorrect}}correct{{/if}} {{/if}} {{/unless}}">
        {{#each items}}
        <div class="gmcq-item item {{#unless ../_isEnabled}} {{#if _isCorrect}}correct{{else}}incorrect{{/if}} {{/unless}} item-{{@index}}">
            <img src="{{image}}" alt="{{alt}}"/>
            <label for="{{../_id}}-{{@index}}" class="{{#if selected}}selected{{/if}}">
                <div class="gmcq-item-state">
                    <div class="gmcq-item-icon gmcq-answer-icon icon"></div>
                    <div class="gmcq-item-icon gmcq-correct-icon icon icon-tick"></div>
                    <div class="gmcq-item-icon gmcq-incorrect-icon icon icon-cross"></div>
                </div>
                <h5 class="gmcq-item-inner">
                    {{{text}}}
                </h5>
            </label>
            <input type="checkbox" id="{{../_id}}-{{@index}}"/>
        </div>
        {{/each}}
        <a href="#" class="button submit">
            <h6>  
                {{{buttons.submit}}} 
            </h6>
        </a>
        <a href="#" class="button reset">
            <h6>
                {{{buttons.reset}}} 
            </h6>
        </a> 
        <a href="#" class="button model">
            <h6> 
                {{{buttons.showCorrectAnswer}}} 
            </h6>
        </a>

        <a href="#" class="button user">
            <h6>
                {{{buttons.hideCorrectAnswer}}} 
            </h6>
        </a>
    </div>
</div>
